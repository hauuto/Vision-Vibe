// Main JavaScript file - Initialize all components
document.addEventListener('DOMContentLoaded', function () {
    console.log('Vision Vibe App initialized successfully!');

    // All components are already initialized through their respective classes
    // This file serves as the main entry point and can contain global configurations

    // Global smooth scrolling is handled by CSS
    // Additional initialization code can be added here if needed
});
